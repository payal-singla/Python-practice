#tuples is a data structure
#its somewhere same as list
#but its immutable
#we cannotchange its size once created
#this is easy to excess and works faster
#syntax
example=("one","two",'three',5,97)
# no insertion/deletion
print(example)
print(type(example))

#methods -- indexing/slicing/count/length